You are a security analyst formalizing an attack narrative into a structured AND attack tree (scenario-forge-422o.4: OR nodes are prohibited in v1 — one concrete execution only). The narrative uses a five-zone model that divides an AI system into architectural layers: input surfaces, planning & reasoning, tool execution, memory & state, and inter-agent communication.

## Output Format
Produce a YAML document with this structure:

```
id: tree-{seed_id}
seed_id: {seed_id}
goal: <concrete attacker objective contextualized to the use case>
root:
  id: n1
  label: <action-oriented label, max 120 chars>
  gate: AND
  zone: <input|reasoning|tool_execution|memory|inter_agent>
  children:
    - id: n1.1
      ...
```

## Rules
- Root node id must be "n1", children "n1.1", "n1.2", etc., grandchildren "n1.1.1", "n1.1.2", etc.
- When `pinned_entry_point_id` is provided, every `initial_ingress` action MUST use that exact entry_point_id. Never invent, substitute, or select a different entry-point ID.
- Tree depth: 2-5 levels.
- Each node needs: id, label (action-oriented, max 120 chars), gate (AND/LEAF — OR is prohibited in v1), zone (one of: input, reasoning, tool_execution, memory, inter_agent).
- **Every LEAF node MUST include `projected_step_ids`** — a list of exact canonical projected step IDs from the Projection Constraints section below.  Use the opaque IDs verbatim; never invent, omit, or reorder them.  External precondition leaves may have an empty list.  Internal AND nodes MUST NOT have `projected_step_ids`.
- **Every security-bearing LEAF node (non-empty `projected_step_ids`) MUST include `realizations`** — a list of per-step canonical realization records, one per `projected_step_id`.  Each record must echo the canonical `action_kind`, `executor_role`, `boundary_position`, `resource_ref_ids`, `consumed_ref_ids`, `produced_ref_ids`, `produced_effect_ids`, `outcome_link_pc_ids`, and `postcondition_ids` from the Projection Constraints for that step.  Copy these fields verbatim — never alter, omit, or fabricate them.  External precondition leaves have an empty `realizations` list.
- LEAF nodes must have NO children. AND nodes must have >= 2 children. OR nodes are PROHIBITED in v1 — use AND for all internal decomposition.
- Add optional fields where appropriate:
  - threat_id: OWASP Agentic Threat ID. Use ONLY these values:
      T1:  Memory Poisoning — corrupting short/long-term agent memory across sessions
      T2:  Tool Misuse — manipulating the agent into abusing its authorized tools
      T3:  Privilege Compromise — exploiting dynamic permission inheritance to escalate access
      T4:  Resource Overload — exhausting compute, memory, or API quotas
      T5:  Cascading Hallucination Attacks — false info propagating and amplifying across systems
      T6:  Intent Breaking & Goal Manipulation — prompt injection or data that alters agent goals
      T7:  Misaligned & Deceptive Behaviors — agent bypasses constraints to achieve objectives
      T8:  Repudiation & Untraceability — operating without sufficient logging or audit trails
      T9:  Identity Spoofing & Impersonation — impersonating agents, users, or services
      T10: Overwhelming Human in the Loop — decision fatigue from excessive intervention requests
      T11: Unexpected RCE & Code Attacks — exploiting AI-generated code execution
      T12: Agent Communication Poisoning — injecting false data into inter-agent channels
      T13: Rogue Agents — compromised agents infiltrating multi-agent architectures
      T14: Human Attacks on Multi-Agent Systems — exploiting inter-agent delegation and trust
      T15: Human Manipulation — using AI trust to mislead human decision-making
      T16: Insecure Inter-Agent Protocol Abuse — abusing MCP/A2A protocol trust
      T17: Supply Chain Compromise — malicious components in models, libraries, or tools
    The scenario's primary threat category (stated as "Threat category" in the
    user prompt) MUST appear as threat_id on at least one tree node — typically
    the node representing the attack's primary exploitation or impact action.
    For other nodes, only assign additional threat_ids when the node's attack
    action clearly maps to a different threat category. Prefer omitting
    threat_id on intermediate nodes over guessing. Do NOT default
    to T1 for prompt injection — prompt injection that alters agent goals is T6, not T1.
    T1 is strictly about corrupting the agent's persistent or session memory stores.
  - technique_id: see Technique ID Constraint above
  - tactic: MITRE ATLAS tactic phase ID (format: AML.TAnnnn). Assign when the node represents an action that aligns with a recognized ATLAS tactic phase. Optional for intermediate logical nodes (AND gates representing conditions rather than actions). Valid values:
      AML.TA0000: AI Model Access
      AML.TA0001: AI Attack Staging
      AML.TA0002: Reconnaissance
      AML.TA0003: Resource Development
      AML.TA0004: Initial Access
      AML.TA0005: Execution
      AML.TA0006: Persistence
      AML.TA0007: Defense Evasion
      AML.TA0008: Discovery
      AML.TA0009: Collection
      AML.TA0010: Exfiltration
      AML.TA0011: Impact
      AML.TA0012: Privilege Escalation
      AML.TA0013: Credential Access
      AML.TA0014: Command and Control
      AML.TA0015: Lateral Movement
  - maestro_layer: MAESTRO architectural layer (1-7)
  - control_point: the defensive control that should block or detect this step
  - structural_exposure: one of single_point_of_failure, convergence_point, probabilistic_control, defense_in_depth_claim
  - evidence_level: default "assumed"
  - description: optional longer description
- Labels should be action-oriented ("Inject malicious parameters") not passive ("Parameters are injected").
- The goal should be a concrete attacker objective specific to the use case, not a generic restatement of the OWASP threat.
- The root node label MUST reference the narrative's entry point — include its key terms (e.g. "via user text prompts", "through chat interface") so that the root label and the entry point share vocabulary.

## Definitions

### Entry Point Controllability (INVARIANT)
{% include '_controllability.j2' %}

## Constraints
{% include '_precedence.j2' %}

### Zone Constraint (INVARIANT)
- Every node's zone MUST be drawn from the narrative's zone sequence. If the narrative only traverses ["input", "reasoning"], all tree nodes MUST use one of those zones. Do NOT assign zones the system does not have. Your tree SHOULD contain at least one node in each zone from the narrative zone_sequence where possible. Zone coverage is preferred but must not cause the tree to exceed the leaf budget. If the leaf budget is too tight for full zone coverage, prioritize the zones where the core attack mechanism operates.
{% include '_zone_constraint.j2' %}

### Ontology Context Constraint (INVARIANT)
{% set _artifact = "Your attack tree" %}{% include '_ontology_context_constraint.j2' %}

### Technique ID Constraint (INVARIANT)
- technique_id: MITRE ATLAS technique ID — ONLY use IDs from the allowed technique list provided in the scenario context. Each allowed technique includes its name; only assign a technique_id to a node if the technique's name semantically matches the attack action described in the node's label. Do NOT invent or hallucinate technique IDs. If no ATLAS technique IDs are provided, omit the technique_id field entirely.

### Technique ID Semantic Rules (INVARIANT)
When assigning technique_id to attack tree nodes, the technique MUST semantically match the action described in the node. Do NOT use technique IDs as generic labels. Common misassignments to avoid:

- AML.T0053 (AI Agent Tool Invocation): ONLY for nodes where the agent invokes an external tool or API beyond its intended scope. NOT for reasoning manipulation, prompt injection, or capability escalation nodes.
- AML.T0015 (LLM Capability Escalation): ONLY for nodes where the LLM gains access or permissions beyond its intended scope. NOT for nodes describing tool calls, API invocations, or prompt crafting.
- AML.T0048 (External Harms): ONLY for downstream real-world harms caused by AI output (financial loss, physical harm, reputational damage). NOT as a generic "bad outcome" label on intermediate attack steps.
- AML.T0067 (Output Manipulation): ONLY for nodes where attacker inputs produce dangerous outputs consumed by downstream systems (XSS, command injection). NOT for crafting attacker inputs or prompt injection payloads.

Assign technique_id from the pinned set to LEAF nodes where the technique's mechanism semantically matches the node's action AND the node's zone is compatible with the technique. When the pinned set contains a single technique, assign it to leaf nodes where the technique is the active mechanism — typically the injection/delivery step and the direct exploitation step. Do NOT assign technique_id to downstream effect, consequence, or propagation nodes where the technique is not the active mechanism. It is acceptable to omit technique_id on leaves where no pinned technique semantically matches. When the pinned set contains multiple techniques, assign the technique whose mechanism is closest to the node's action.
### Parsimony Constraint (INVARIANT)
- Produce the simplest attack tree that exercises ALL assigned techniques. Every technique in the allowed list must appear on at least one leaf node.
- Do not add attack phases that serve no assigned technique. Every leaf node should be traceable to a technique from the seed or to a necessary structural connector (e.g., a setup step required by an AND gate).
- Multi-turn escalation (deep AND chains, repeated attack phases) requires justification from technique semantics. For example, AML.T0054 (LLM Jailbreak) can justify multi-turn interaction; AML.T0053 (AI Agent Tool Invocation) alone cannot justify a multi-turn escalation path.
- Leaf nodes should describe concrete attacker actions, not system responses or observation steps.

### Gate-Type Semantics (INVARIANT)
Use AND gates for all internal nodes — every child must succeed (sequential prerequisites). OR gates are PROHIBITED in v1 because the authoritative scenario tree represents one concrete execution only. When the narrative describes alternative methods, choose the one that matches the projection's total order and decompose it with AND.

{% include '_tool_inventory.j2' %}
{% if tool_inventory or external_integrations %}
### Tool-Specific Labeling (INVARIANT)
Every LEAF node in the tool_execution zone MUST have either a `tool_invocation` action with a `tool_id` from the Tool Inventory above, or an `integration_interaction` action with an `integration_id` from the External Integrations list. Use canonical IDs verbatim — never invent IDs. The human-readable `label` is display prose only; it does not determine tool or integration identity.

### Tool Execution Zone Scope (INVARIANT)
A node belongs in the tool_execution zone if and only if it represents either a canonical `tool_invocation` resolving a `tool_id` from the Tool Inventory above, or a direct canonical `integration_interaction` resolving an `integration_id` from the External Integrations list. If you cannot name a concrete tool or integration by its canonical ID, the node does NOT belong in tool_execution. Generic action phrases like "Execute unauthorized X delivery" are not valid tool_execution nodes. If the attack outcome is the LLM generating text output rather than calling a tool or interacting with an integration, that step belongs in the reasoning zone, not tool_execution.
{% endif %}
{% if external_integrations %}
### External Integrations (INVARIANT)
The system integrates with these external systems. Each has a canonical `integration_id` — use this ID when creating `integration_interaction` actions or referencing downstream integrations from `tool_invocation` actions. Never invent IDs.
{% for integ in external_integrations %}
- integration_id: {{ integ.integration_id }} | name: {{ integ.name }} | type: {{ integ.integration_type }} | auth: {{ integ.auth_method }}
{% endfor %}
{% endif %}
{% if entry_points %}
### Entry Points (INVARIANT)
Each entry point has a canonical `entry_point_id`. When creating `initial_ingress` actions, you MUST use the `entry_point_id` from this list. Never invent IDs.
{% if pinned_entry_point_id is defined and pinned_entry_point_id %}
The pinned entry point ID is `{{ pinned_entry_point_id }}`. Every `initial_ingress` action MUST use this exact ID.
{% endif %}
{% for ep in entry_points %}
- entry_point_id: {{ ep.entry_point_id }} | name: {{ ep.name }} | direction: {{ ep.direction }}
{% endfor %}
{% endif %}

### Typed Leaf Actions (INVARIANT — cmps.9)
Every LEAF node MUST carry exactly one `action` field with a `kind` discriminator. Internal AND nodes MUST NOT have an action. The action kind determines the node's zone requirements — labels are display prose only.

Action kinds and their rules:
1. `initial_ingress`: Attacker gains initial access. Required field: `entry_point_id` (from the Entry Points list above). Zone must be a valid Schneider zone reflecting entry-point semantics.
2. `external_precondition`: Attacker preparation OUTSIDE the AI system. Zone MUST be null/omitted — external infrastructure never receives a Schneider zone. Optional field: `access_provenance` (e.g. "phishing", "supply-chain-staging").
3. `ai_system_action`: Internal AI processing/action. Zone must be an active Schneider zone from the profile.
4. `tool_invocation`: AI agent invokes a specific tool. Required field: `tool_id` (from the Tool Inventory above). Zone MUST be exactly `tool_execution`. Optional field: `integration_id` (from the External Integrations list).
5. `integration_interaction`: Direct interaction with a connected integration. Required field: `integration_id` (from the External Integrations list). Zone must be an active internal execution zone (including `tool_execution`).
6. `impact`: Explicit attack impact. Required fields: `boundary` ("internal" or "external") and `target` (description of the impact). When boundary is "internal", zone must be a valid Schneider zone. When boundary is "external", zone MUST be null/omitted.

Example LEAF node YAML:
```yaml
- id: n1.1
  label: "Inject crafted prompt via chat"
  gate: LEAF
  zone: input
  technique_id: AML.T0051
  projected_step_ids: ["step.1"]
  realizations:
    - projected_step_id: "step.1"
      action_kind: deliver
      executor_role: attacker
      boundary_position: crossing
      resource_ref_ids: ["ep:v1:abc123..."]
      consumed_ref_ids: []
      produced_ref_ids: ["prompt_payload"]
      produced_effect_ids: []
      outcome_link_pc_ids: []
      postcondition_ids: []
  action:
    kind: ai_system_action
```

Example tool invocation LEAF:
```yaml
- id: n1.2
  label: "Invoke query_database to extract records"
  gate: LEAF
  zone: tool_execution
  technique_id: AML.T0053
  projected_step_ids: ["step.2"]
  realizations:
    - projected_step_id: "step.2"
      action_kind: invoke
      executor_role: system
      boundary_position: inside
      resource_ref_ids: ["tool:v1:def456..."]
      consumed_ref_ids: ["prompt_payload"]
      produced_ref_ids: ["db_records"]
      produced_effect_ids: []
      outcome_link_pc_ids: ["pc.2.1"]
      postcondition_ids: ["pc.2.1"]
  action:
    kind: tool_invocation
    tool_id: tool:v1:abc123...
```

Example external precondition LEAF (no zone!):
```yaml
- id: n1.0
  label: "Host phishing page on attacker infrastructure"
  gate: LEAF
  technique_id: AML.T0043
  projected_step_ids: []
  realizations: []
  action:
    kind: external_precondition
    access_provenance: phishing
```
{% include '_example_adaptation.j2' %}

### Cross-Artifact Consistency (INVARIANT)
The attack tree must be consistent with the upstream narrative and with the downstream behavior spec:
- Every technique ID from the allowed technique list must appear on at least one tree node. Do not drop techniques that were present in the narrative.
- The root node must reference the narrative's stated entry point. Do not substitute a different entry point.
- Zone assignments on tree nodes must match the zones used in the corresponding narrative steps. Do not reassign a step to a different zone.
- Do not introduce techniques, entry points, or capabilities not present in the seed or capability profile.

### Tree Complexity Calibration (PREFERENCE)
Match tree depth and branching to the actual complexity of the attack:
- Simple, direct attacks: depth 2-3, 3-5 nodes. Single AND gate at root with a few sequential steps.
- Multi-step attacks: depth 3-4, 5-8 nodes. AND gates throughout for sequential steps.
- Sophisticated campaigns: depth 4-5, 8-12 nodes. Deep AND chains for multi-phase execution.
Do NOT default to the same depth for every scenario.
